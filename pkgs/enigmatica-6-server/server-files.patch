diff --git a/server-files/server_files/server-setup-config.yaml b/server-files/server_files/server-setup-config.yaml
index 4db6c6a4..6ba56c85 100644
--- a/server-files/server_files/server-setup-config.yaml
+++ b/server-files/server_files/server-setup-config.yaml
@@ -108,10 +108,10 @@ launch:
     checkOffline: no

     # specifies the max amount of ram the server is supposed to launch with
-    maxRam: 5G
+    maxRam: 8G

     # specifies whether the server is supposed to auto restart after crash
-    autoRestart: yes
+    autoRestart: no

     # after a given amount of crashes in a given time the server will stop auto restarting
     crashLimit: 10
@@ -163,3 +163,4 @@ launch:
         - '-Daikars.new.flags=true'
         - '-Dfml.readTimeout=180' # servertimeout
         - '-Dfml.queryResult=confirm' # auto /fmlconfirm
+        - '-XX:+UseStringDeduplication'
diff --git a/server-files/server_files/start-server.sh b/server-files/server_files/start-server.sh
index a1daab5a..18149060 100644
--- a/server-files/server_files/start-server.sh
+++ b/server-files/server_files/start-server.sh
@@ -1,4 +1,5 @@
 #!/bin/bash
+cd /srv/minecraft
 DO_RAMDISK=0
 if [[ $(cat server-setup-config.yaml | grep 'ramDisk:' | awk 'BEGIN {FS=":"}{print $2}') =~ "yes" ]]; then
     SAVE_DIR=$(cat server.properties | grep 'level-name' | awk 'BEGIN {FS="="}{print $2}')
